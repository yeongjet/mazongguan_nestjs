{
  "name": "@mazongguan/bp",
  "version": "0.0.1",
  "scripts": {
    "build": "rimraf dist && babel --extensions .ts --ignore node_modules dist . -d ./dist",
    "start": "babel-node --extensions .ts --config-file ./babel.config.js -- src/main.ts --debug",
    "test": "jest"
  },
  "dependencies": {
  }
}
